---
title: "Install Node.js"
description: "Quick guide to install Node.js for your development environment"
---

# Install Node.js

Node.js is required to run your React Native app and manage packages. Here's how to install it:

## Step 1: Download Node.js

1. Go to [nodejs.org](https://nodejs.org/en/download)
2. Download the **LTS version** (recommended for most users)
3. Choose your operating system:
   - **Windows**: Download the Windows Installer (.msi)
   - **macOS**: Download the macOS Installer (.pkg)
   - **Linux**: Download the Linux Binaries or use your package manager

## Step 2: Install Node.js

### Windows & macOS:
1. Run the downloaded installer
2. Follow the installation wizard
3. Accept the license agreement
4. Keep all default settings
5. Click "Install" and wait for completion

### Linux (Ubuntu/Debian):
```bash
# Update package index
sudo apt update

# Install Node.js
sudo apt install nodejs npm

# Or use the official repository for latest version
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt-get install -y nodejs
```

## Step 3: Verify Installation

Open your terminal/command prompt and run:

```bash
node --version
```

You should see something like `v18.17.0` or higher.

Also check npm (comes with Node.js):

```bash
npm --version
```

You should see something like `9.6.7` or higher.

## Troubleshooting

**Command not found?**
- Restart your terminal/command prompt
- On Windows, restart your computer
- Make sure you selected "Add to PATH" during installation

**Need to update?**
- Download the latest version from [nodejs.org](https://nodejs.org)
- Or use npm: `npm install -g npm@latest`

## Next Steps

✅ Node.js is installed! You can now continue with the [Project Setup](/quickstart/project-setup) guide.
